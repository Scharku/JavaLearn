package FullLesson;
public class Lesson12 {
    /* 
    На 12 уроке начались циклы.
    
    Конструкция if
    if (boolean expression) 
        { our code }
    
    Конструкция if-else
    if (boolean expression) 
        { our code }
    else 
        { our code }
    
    Конструкция if-else-if
    if (boolean expression)
        { our code }
    else if (boolean expression)
        { our code }
    else 
        { our code }
    
    Конструкция if-if-else
    if (boolean expression)
    if (boolean expression)
        { our code }
    else
        { our code }
    else
        { our code }
    
    equals.
    Нельзя сравнивать объекты, так как при создании объекта ему адется уникальный
    адрес, из-за этого сравнения типа объект1 == объект2, всегда будет возвразаться
    false. Чтобы сравнить содержимое, нужно использовать equals(), например
    s1.equals(s2), тогда, если содержимое одинаковое, то будет true.
    equals есть всегда, но по умолчанию можно сравнить только String, созданные
    объекты самим надо объяснять джаве, чем они равны между собой.
    
    Ternary operator.
    (boolean expression) ? (if true) : (if false)
    Например - 
    int a = 5; int b = 10
    int maximum = (a>b) ? a : b;
    sout(maximum);
*/
}


